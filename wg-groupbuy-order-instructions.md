# WG团购群 — 生成新一场团购的 JSON 文件

把这份文件、`product-catalog.json`（商品短键参考表）和你要处理的接龙记录
一起发给 Claude（新建一次对话即可，不需要项目/Project），让它帮你生成
两个文件：`data-<日期>.json` 和 `manifest.json` 里要新增的那一行。生成
好之后把这两样发回来，我这边负责上传到 GitHub 部署。

**记得把 `product-catalog.json` 也一起发过去**——里面有全部 154 个已用
过的商品短键，附带中文名、分类、上次价格、称重/整箱分摊方式（见下方
"称重/箱分摊字段"）。这样新商品才会正确沿用已有短键，而不是被重新起
一个不一样的键。

---

## 交给 Claude 的任务说明（可以直接整段复制粘贴给它）

> 请根据下面贴的接龙记录，帮我生成一个 WG团购群团购看板用的
> `data-<日期>.json` 文件，以及要加进 `manifest.json` 的那一行。
>
> **`data-<日期>.json` 的结构：**
>
> ```json
> {
>   "groupName": "WG团购群",
>   "itemsLabel": "小馄饨团购",
>   "products": {
>     "sig": { "label": "招牌鲜肉馄饨", "price": 6.5 },
>     "pork_belly": { "label": "五花肉", "price": 12.0, "unit": "kg" },
>     "peach": { "label": "彩虹油蟠桃", "price": 8.0, "unit": "kg", "weighMode": "weight", "gramsPerUnit": 1000 },
>     "grape_jufeng": { "label": "辽宁巨峰(3串/箱)", "price": 25.0, "weighMode": "proportional" },
>     "pear_box": { "label": "香梨(4粒/份)", "price": 12.0, "piecesPerUnit": 4 }
>   },
>   "orders": [
>     { "name": "小明", "items": { "sig": 3 } },
>     { "name": "小华", "items": { "sig": 1, "shrimp": 1 } },
>     { "name": "小美 & 阿杰", "items": { "corn": 2 } }
>   ]
> }
> ```
>
> **规则：**
> 1. `groupName` 固定写 `"WG团购群"`。
> 2. `itemsLabel` 是这场团购的一句话说明（例如"小馄饨团购"），用在到货
>    通知按钮上；如果这场商品种类很少，直接把商品名连起来读着通顺，
>    也可以不写这个字段。
> 3. `products` —— 给每个商品起一个英文短键（snake_case，例如
>    `sig`、`shrimp`、`pork_belly`），值是 `{ label: 中文名, price,
>    unit?（可选，默认是"盒"，按重量或其他单位卖的要写清楚，比如
>    "kg"）}`。**如果这场用到的商品名字和以前某场团购里的商品是同一样
>    东西，请沿用同一份 `product-catalog.json` 里的短键**（比如"招牌
>    鲜肉馄饨"一直用 `sig`）——这个文件已经把 154 个用过的短键、称重/
>    箱分摊方式都整理好了，一起发给你参考；确实是新商品才起新键，并
>    且新商品也要按下面"称重/箱分摊字段"的规则判断要不要加
>    `weighMode`/`gramsPerUnit`/`piecesPerUnit`。
> 4. `orders` —— 每人一条记录，`name` 照抄接龙里的名字（含表情符号、
>    符号都保留），`items` 是 `{短键: 数量}`。数量可以是小数（比如
>    半份/半公斤写 `0.5`）。
> 5. **两人拼单、一起付一笔钱的**，合成一条记录，`name` 写成
>    `"小美 & 阿杰"` 这样用 `&` 连接的字符串，不要拆成两条。
> 6. **价格还没确定或商品对不上价目表的**，仍然把它加进那个人的
>    `items` 里，但在 `products` 里把这个商品的 `price` 设为 `null`，
>    并加一个字段 `"unverified": true`。
> 7. 帮我核对总数：每个人的小计、每种商品的总数量、总金额，用脚本算
>    一遍，不要手工加总，避免数错。
> 8. 遇到"原味"/"鲜肉"这种模糊说法，按老规矩当成招牌口味（`sig`）
>    处理，但如果你不确定，跟我说一声，不要自己悄悄猜。
> 9. **称重/箱分摊字段（`weighMode`/`gramsPerUnit`/`piecesPerUnit`）：**
>    这三个字段决定送货当天在看板上能不能直接用就地编辑器改数量，
>    **优先设置这些字段，而不是自己手工换算单价**：
>    - **按重量卖、或整包按重量分装**（例如 kg 装水果、"2kg/包"的
>      柑橘）：`weighMode: "weight"`，`gramsPerUnit` 填一份对应多少
>      克（"kg"商品填 1000，"2kg/包"商品填 2000）。送货当天只需要
>      输入实际到手克数，看板会自动按 **10g** 向下取整算出正确数量。
>    - **一箱/一份按重量分给几个人、但每人分到的重量不固定**（例如
>      葡萄论串卖，一箱几串分给几个人，每串轻重不一样）：
>      `weighMode: "proportional"`，`price`/`unit` 照抄整箱的价格，
>      **不要**换算成单价。送货当天输入这一份的净重和全箱总重，看板
>      会自动按比例算出应付金额。
>    - **一份/一箱按固定颗数分装，可能部分损坏或短少**（例如"4粒/
>      份"的梨）：加 `piecesPerUnit`（这份/这箱有几颗）。送货当天
>      直接填实收颗数，看板按比例折算金额。
>    - **当场就能直接数清楚、不需要再称重**的商品（比如一箱按串数
>      分、每人拿几串是当场直接数出来的）：不需要这几个字段，可以
>      直接把整箱价格换算成"每串/每份"的单价，`unit` 写成对应量词，
>      每人 `items` 填实际拿到的数量——这种情况和普通商品按份定价
>      没有区别。
>    - 拿不准该用哪一种、或者不确定这个商品到底是"当场数清"还是
>      "当天称重"，跟我确认一下，不要自己假设。`product-catalog.json`
>      里同名商品之前是怎么分类的，直接沿用即可。
>
> 生成完 `data-<日期>.json` 之后，再单独给我一行要加进
> `manifest.json` 的内容，格式是：
> ```json
> { "date": "2026-XX-XX", "label": "X/X", "file": "data-2026-XX-XX.json" }
> ```
> 日期用接龙里写的送货/团购日期；如果记录里没写清楚日期，问我，不要
> 自己猜。
>
> 最后把这两样东西都作为**可下载的文件**给我（不只是聊天里贴文字），
> 我需要把文件存到 iCloud 文件夹里。
>
> 这是接龙记录：
> [把接龙原文贴在这里]

---

## 以前用过的商品短键（发 `product-catalog.json` 给 Claude 对照，避免重复造键）

商品短键的完整参考表就是 `product-catalog.json`——里面有全部 154 个已
用过的短键，每个都带中文名、分类、上次价格、`unit`，以及是否需要
`weighMode`/`gramsPerUnit`/`piecesPerUnit`（已经按上面"称重/箱分摊字
段"的规则分类好了）。**把这个文件和这份说明一起发给 Claude**，不要凭
记忆或猜测起新短键——这个文件比这份说明里能贴的任何小表格都更完整、
更新。

（这个文件是 Claude 内部维护用的参考表，从不部署到 GitHub 网站上，
`index.html` 也不会读取它；纯粹是给下一次新建团购数据文件时对照用的。
如果这一场用到的商品完全没在里面，用完之后请把新商品发给我，我这边
更新这个文件。)

## 不用管的两个文件：`product-emoji-map.json` 和 `message-template.json`

这两个文件是**所有场次共用**的设置，正常情况下每次新增一场团购都不
需要碰它们：

- **`product-emoji-map.json`**——商品名前面自动带的表情图标（🍑水
  蜜桃、🐔鸡肉、🥟馄饨……）就是靠这个文件按关键词自动匹配的，目前已
  经覆盖了馄饨、大部分水果蔬菜肉类。新商品只要名字里带得到已有关键
  词，会自动配到对应图标，不用手动设置。如果新商品完全匹配不上（会
  显示默认的 🛒 图标），不算出错，把商品名告诉我就行，我这边加个新
  关键词进去。
- **`message-template.json`**——控制"复制付款消息""复制到货通知"
  这些按钮生成的文字措辞（问候语、结尾语等），是固定设置好的，不会
  因为某一场团购而改变，除非你想统一调整措辞的时候才需要改它。

所以正常流程只需要生成 `data-<日期>.json` 和 `manifest.json` 里的
新增行这两样东西就够了。

## 完成后怎么给我

把 Claude 生成的两个文件（`data-<日期>.json` 的完整内容 + 要加进
`manifest.json` 的那一行）保存/拖进我们共享的 iCloud 文件夹就行，我
这边负责合并进 `manifest.json` 并推送到 GitHub 部署。
